# Python 重学微课｜V1 教学内容蓝图

文件：`v1-content-spec.md`  
版本：1.0 · 2026-09-23  
交付对象：Muse Code / Coding Agent / 内容 Agent  
配套文件：`python_relearn_kindle_v1_spec.md`（以下简称“开发规格”）

> 本文决定“教什么、先教什么、用什么例子、怎样证明学会”，不修改产品、技术架构或教材协议。保留开发规格中的 16 个课程 ID、原有顺序、每课两道小测及四项模块实践。[B1] 本文是正式教材的写作依据，不是完整课文，也不是 YAML 正文。

## 1. 教学目标与执行边界

### 1.1 V1 结束时的能力

学习者应能够面对一个输入、规则和输出均已明确的小任务，独立完成四件事：**逐行追踪代码；解释结果的原因；根据规则修改代码；从空文件写出可测试的小脚本。**

“独立”允许查已经学过的语法和官方文档，但首次尝试不让 AI 代写完整解法。先写预测、实现或错误定位，再向 ChatGPT 提交自己的判断求助。此要求是学习方法，不增加网站功能或学习门禁。

结业任务是读取本地文本并生成可重复验证的词频报告；不是独立开发完整数据平台，也不是通过所有 Python 面试题。工作与面试关联只说明能力用途，不宣称某知识点的实际出题频率。

### 1.2 固定的内容取舍

示例与正式教材统一使用 Python 3.13；这是项目基线，不是“最新版”的判断。V1 只使用内置能力和必要标准库，不依赖网络服务、真实凭据或第三方数据。

首轮明确不讲：类与继承、装饰器、递归专项、迭代器协议与生成器、异步与并发、类型标注体系、正则表达式、NumPy/pandas、CSV/JSON、HTTP、命令行参数框架、复杂度与算法专项。后续课程可以接续，但不得为了“铺垫”提前塞入本轮。

课程中的“记录”“清洗”“报告”都要给出具体规则。不能用这些词代替输入约束；不能擅自把去空格升级成模糊匹配，把词频升级成中文分词。

### 1.3 三档教学深度

**掌握**：必须有解释、示例和独立迁移要求。  
**识别**：能够读懂当前用途即可，通常只用一句说明或一个对照。  
**延后**：本课不展开，不进入本课小测和实践参考解法。

每课只有一个主要能力目标。允许几个相关操作服务于同一目标，但不得把“必须解释”写成 API 清单。

## 2. 16 课的递进与依赖

### 2.1 课程主线

M1：一份数据怎样变成结果。  
M2：一批数据怎样被遍历、组织、统计。  
M3：怎样把处理过程拆成不会意外影响彼此的函数。  
M4：怎样把函数接上文件、运行环境、错误处理与测试。

| 顺序 | 保留的课程 ID | 本课能力锚点 | 最小直接前置 |
|---|---|---|---|
| 01 | `run-and-bindings` | 追踪名字与当前值 | 无 |
| 02 | `types-and-none` | 分清值的类型与缺失含义 | 01 |
| 03 | `strings` | 把原始文本变成规范文本 | 02 |
| 04 | `conditions` | 把规则翻译为唯一执行路径 | 02、03 |
| 05 | `loops` | 追踪重复过程及其终止边界 | 04 |
| 06 | `lists-tuples` | 保存并逐项处理有序数据 | 03、05 |
| 07 | `dicts` | 按键维护记录与频次 | 06 |
| 08 | `sets` | 区分“出现过”与“出现几次” | 07 |
| 09 | `functions` | 明确输入、返回值与调用过程 | 08 |
| 10 | `scope-defaults` | 分清每次调用与定义时留下的状态 | 02、06、09 |
| 11 | `alias-copy` | 判断修改会影响哪些名字和容器 | 10 |
| 12 | `comprehensions-sort` | 清楚地表达转换与排序规则 | 07、09、11 |
| 13 | `exceptions` | 定位失败并仅处理预期异常 | 02、05、09 |
| 14 | `paths-text` | 把文本文件正确接入处理流程 | 03、06、13 |
| 15 | `modules-venv` | 让处理函数可导入、程序可明确运行 | 09、14 |
| 16 | `tests-boundaries` | 用明确预期验证程序及边界 | 12、13、15 |

前置列用于解释直接依赖，不表示可以跳过其他课。正式内容按 01—16 顺序编写；课程 ID 保持不变，不能改用编号作为永久身份。

### 2.2 必须消除的“知识偷跑”

| 容易发生的问题 | 本蓝图固定的处理方式 |
|---|---|
| 循环课在列表课之前 | 05 只用已学字符串及当课解释的 `range`；06 再迁移到列表 |
| 字符串方法返回列表 | 03 不教 `split`；06 学完列表后再教无参数 `split()` |
| 自定义函数尚未讲到，却必须调用内置能力 | 01 解释“名字后加括号是在调用操作”；03 解释 `值.方法()` 的读法；09 再讲定义、参数及返回机制 |
| 默认参数涉及可变对象，完整别名课在后 | 10 用 01 的绑定模型和 06 的列表修改解释默认对象复用；11 再统一解释别名、传参和浅拷贝 |
| 文本文件课需要 import，但模块课在后 | 14 就地解释 `from pathlib import Path` 是“从标准库取出路径工具”；15 才讲模块执行、入口与环境 |
| 排序 key 常被直接写成 lambda | 12 固定先用已学的命名函数；本轮不教 lambda、复合排序键或多轮排序 |
| 词频报告突然需要拼接多行文本 | 14 解释 `"\n".join(字符串列表)`；16 实践只复用，不临时引入 |
| 早期实践要求处理任意输入 | P1 只处理题目声明的整数文本；非数字转换失败到 13 再处理，不提前引入异常 |

### 2.3 贯穿全程的五条理解线

**绑定与修改**：01 重新绑定 → 03 字符串不原地修改 → 06 列表可修改 → 09 参数 → 10 默认对象 → 11 别名与拷贝 → P3 输入不被修改。

**缺失与空值**：02 `None` 不等于零 → 04 真值与缺失判断 → 07 键缺失 → 09 默认返回 `None` → 10 `None` 默认参数 → 13 转换失败 → 16 零值、空输入测试。

**遍历与顺序**：03 切片边界 → 05 循环边界 → 06 有序列表 → 07 字典插入顺序 → 08 集合不承担顺序 → 12 稳定排序 → P4 同频词的固定顺序。

**输入—处理—输出**：P1 单份数据 → P2 批量统计 → P3 拆成函数 → 14 文件输入输出 → 15 模块与入口 → P4 可测试脚本。

**先定义正确，再写代码**：04 阈值 → P2 去重与计数规则 → P3 保留零与不修改输入 → 13 失败规则 → 16 用独立预期检验。

## 3. Kindle 微课的内容写法

每课默认七步：①旧知识唤回与目标；②核心机制；③先预测再展开的主例；④一个关键对照或误区；⑤小测一；⑥小测二；⑦回忆与独立任务。04、08、12、16 在末尾附模块实践。各课下文的“教学顺序”指定内容先后，不要求每个箭头各占一步。

每步围绕一个问题；正文目标 100—250 汉字，代码通常 4—10 行。不能用一个含多种新语法的“一行写法”代替解释。较长实践解法按已有函数或处理阶段分块，不把完整程序挤进一个 Kindle 阅读块。[B1]

主例的解析必须回答：**运行到哪里？当前值是什么？这一行做了什么？为什么不是另一种结果？** 需要状态追踪时用少量文本，如“第二轮：当前项 sql，旧次数 0，新次数 1”，不用宽表、对象地址或复杂内存图。

每课两道正式小测的场景与判定锚点由本文锁定。第一题主要诊断阅读与解释，第二题主要诊断修改、选择正确实现或迁移；允许少量环境诊断题例外。每个错误选项对应一个明确误解，不用“以上都对”、未讲语法或纯措辞陷阱。下面给出的是出题蓝图，正式 Agent 仍需编写完整题干及全部选项解析。

“独立任务”是总结中的短迁移建议，不新增第五项模块实践，不增加完成状态。模块实践在 PyCharm 完成；Kindle 不承担长代码输入。

---

## 4. M1｜读懂基本代码

### 01｜运行脚本、变量绑定
**ID：`run-and-bindings`**

**核心能力**：对一个没有分支的短脚本，逐行说出当前值和最终输出，知道重新运行会从头开始，而不是接着上次的变量状态继续。

**必须讲清**：脚本文件与运行输出是两件事；语法正确的普通顶层语句按顺序执行；赋值先求右侧结果，再让左侧名字绑定结果；`x = x + 1` 不是数学等式；`print` 用于观察结果。变量可类比“名字标签”，不能一开始就讲成自动复制内容的独立盒子。[P1]

**范围**：掌握赋值、简单整数运算、字符串字面量、`print`、注释。识别函数调用的括号。延后类型系统、输入交互、列表共享、函数定义、IDE 插件和复杂配置。课前只要求能在 PyCharm 普通脚本运行模式运行一个 `.py` 文件，不复用交互控制台中的旧变量；不把虚拟环境原理前置到这里。

**教学顺序与主例**：运行一个显示文件名和份数的脚本 → 分清代码与输出 → 跟踪 `copies = 2`、`saved = copies`、`copies = copies + 1` → 预测两个名字的值 → 只改一个输入重新运行。解释 `saved` 没有成为“随 copies 自动更新的公式”。

**易误解**：赋值会建立永久联动；没有 `print` 就等于没有计算；编辑文件后旧输出会自动变化；运行不同文件却把结果当作当前文件的输出。

| 小测 | 固定出题设计 |
|---|---|
| `run-and-bindings-q1` | 依次给出 `copies = 2`、`saved = copies`、`copies = copies + 1`、`print(saved, copies)`。正确输出 `2 3`。干扰项分别诊断“两个名字永久联动”“赋值只执行一次”“左右两侧同时更新”。 |
| `run-and-bindings-q2` | 已有 `price = 20`、`copies = 2`，需求是多买一份后计算总价。选出先更新 copies、再计算 total 的顺序；正确总价为 60。重点不是心算，而是中间结果不会自动重算。 |

**工作联系与独立产出**：能判断 AI 给出的短脚本哪里读取旧值、哪里覆盖新值；独立写出显示资料名称、份数和整数总价的短脚本。口头检查：“改了 copies，之前计算过的 total 为什么不会自己变化？”

### 02｜基本类型、转换与 None
**ID：`types-and-none`**

**核心能力**：在运算前判断拿到的是数字、文本、真假还是缺失值，并选择明确的转换或缺失表达。

**必须讲清**：类型属于值所对应的对象，不是变量名字永久自带的属性；`int`、`float`、`str`、`bool` 和 `None` 各解决什么问题；`"3"` 与 `3` 不同；转换产生结果，不会自动改掉原名字；`None` 表示约定的缺失或无结果，不是字符串 `"None"`。[P2][P3]

**范围**：掌握 `type`、`int`、`str` 的基本用途；识别 `float`、布尔字面量、`/` 与正整数例子中的 `//`。说明浮点数不等于精确十进制，暂不展开表示误差；不讲位运算、复数、布尔值参与算术、复杂强制转换。这里不使用 `input()`；输入先用给定变量模拟。

**教学顺序与主例**：份数 `"3"` 为什么不能直接当数量 → 对照 `"3" + "2"` 与 `int("3") + 2` → 保留原文本、另存数量 → 区分“零份”“尚未填写份数”“文字 None” → 为下一课格式化输出做准备。转换失败只展示 `ValueError` 的名字和含义，处理方法留到 13。

**易误解**：看起来像数字就能参与相同运算；调用 `int(raw)` 会就地修改 raw；所有转换都成功；零和缺失可以互换；`int` 等于四舍五入。

| 小测 | 固定出题设计 |
|---|---|
| `types-and-none-q1` | `raw = "3"`，`count = int(raw)`，分别判断 raw 与 count 的类型，并判断 `count + 2`。正确为 `str`、`int`、5。干扰项围绕原值被转换、字符串拼接和隐式转换。 |
| `types-and-none-q2` | “未填写数量”与“明确填写 0”必须区分，选出分别使用 `None` 和 `0` 的数据设计。不能把空文本、`"None"` 或 0 当作完全等价替代。此题不要求尚未讲过的分支。 |

**工作联系与独立产出**：为未来文本字段、配置和 API 数据提供类型意识；独立把给定的整数文本转换为数量，同时保留原始值，并解释为什么空值处理不能简单归零。口头检查：“变量显示为空，能否直接判断它就是 None？”

### 03｜字符串、切片与格式化
**ID：`strings`**

**核心能力**：把一段原始文本规范化，并解释索引、切片和字符串操作究竟返回了什么。

**必须讲清**：字符串按位置访问，索引从 0 开始；切片取左边界、不取右边界；单个越界索引与超界切片行为不同；字符串不能原地改一个字符；`strip()`、`lower()` 的返回值需要被使用，不能说成“原字符串已经被清洗”。[P4]

**范围**：掌握 `len`、`[0]`、`[-1]`、`[start:stop]`、`strip()`、`lower()`、基础 f-string；`.2f` 只识别为显示两位小数。就地解释方法调用读法。延后切片步长、反转技巧、正则、编码原理、`split`、格式化宽度和对齐大全。

**教学顺序与主例**：原始文件名 `" Report.TXT "` → 先去两端空白，再转小写 → 用 `"report.txt"` 展示 `[0:6]` 和 `[-3:]` → 对照未接收 `strip()` 结果的错误 → 把名称与整数费用嵌入说明。`strip()` 只去两端空白，不删除单词之间的空格。

**易误解**：右端下标也包含在切片内；字符串方法默认原地修改；`strip()` 会删除所有空格；`f"{total:.2f}"` 会把 total 本身改为字符串或解决货币计算精度。

| 小测 | 固定出题设计 |
|---|---|
| `strings-q1` | 给定 `name = "report.txt"`，判断 `name[0:6]` 与 `name[-3:]`。正确是 `"report"`、`"txt"`。干扰项只围绕切片右边界和负索引。 |
| `strings-q2` | 给定 `raw = " Report.TXT "`，要求 clean 为 `"report.txt"` 且 raw 保留原值。正确方案必须接收每次处理结果；可用两行避免未解释的长链。不能选只调用方法、不保存结果的方案。 |

**工作联系与独立产出**：能够读懂文件名、标签或文本字段的规范化代码；独立输出清洗后的资料名称及 `68.00` 形式的费用文本。口头检查：“raw.strip() 执行过，为什么 raw 仍可能带空格？”

### 04｜布尔表达式与条件分支
**ID：`conditions`**

**核心能力**：把一条有边界的业务规则写成可预测的执行路径，而不是凭感觉排列多个 `if`。

**必须讲清**：比较得到判断结果；缩进决定代码属于哪个分支；`if/elif/else` 在同一链中最多执行一个分支，多个独立 `if` 不同；`and/or/not` 的常用条件组合与短路；`0`、空字符串、`None` 都可能为假，但含义不相同；判断是否缺失使用 `is None`。[P5]

**范围**：掌握比较、分支、简单布尔组合；用一句准确说明 `and/or` 不保证返回布尔值，但本课仅组合条件，不教它们充当取默认值的技巧。不讲三元表达式、`match`、位运算或复杂优先级。复合条件优先加括号。

**教学顺序与主例**：资料费用达到 100 元或是会员时免服务费 → 先判断规则边界 → 比较单链与两个独立判断 → 用 `count = 0` 对照 `count is None` 与 `not count` → 完成 P1。短路用“先确认字符串非空，再取首字符”解释，不引入新容器。

**易误解**：`>=` 与 `>` 无区别；`if/elif` 会把所有满足条件的分支都执行；“为假”就代表缺失；在 Python 条件中用 `&` 代替 `and`。

| 小测 | 固定出题设计 |
|---|---|
| `conditions-q1` | 对 `total = 100`，先测 `total >= 100` 输出“免服务费”，`elif total >= 50` 输出“优惠”。正确只输出前者；干扰项为两者都输出、落到第二分支或不输出。 |
| `conditions-q2` | count 可以是 0 或 None；只有 None 应显示“未填写”。选择 `count is None`，并解释 `not count` 会误伤 0。不得借此讲完整对象身份机制，11 再展开。 |

**工作联系与独立产出**：能检查阈值、资格、空值分支的漏洞；独立完成 P1，并说明零份与缺失、达到阈值与未达到阈值的不同路径。口头检查：“为什么不能把所有 if 都机械地改成 elif？”


## 5. M2｜操作常用数据

### 05｜for/while 与循环边界
**ID：`loops`**

**核心能力**：逐轮追踪“当前项、累计状态和下一次是否继续”，能说明循环为何执行这些次数、何时结束。

**必须讲清**：`for` 依次取得元素，不是天然只遍历下标；`range(n)` 不包含 n；累计量在循环外初始化；`while` 每轮开始前检查条件，循环体必须有机会让条件变假；缩进位置决定某操作执行一次还是每轮执行。[P6]

**范围**：掌握字符串遍历、`range` 的一参数/两参数形式、计数与累加、简单 `while`。识别 `+=`、`-=`、`break` 结束当前循环、`continue` 跳过本轮余下部分。延后列表、嵌套循环、循环 `else`、迭代器原理和复杂重试。

**教学顺序与主例**：在 `"EIEE"` 中数 E → 手工列出每轮 count → 用 `range(1, 4)` 输出任务编号 → 用 remaining 从 3 递减解释 `while` → 对照初始化或递减放错缩进的位置。`break/continue` 各用一句与短对照，不扩成新的主案例。

**易误解**：range 的终点包含在内；空循环也执行一次；累计量每轮清零还能得到总数；递减写在循环外也能让循环结束；`continue` 会跳出整个循环。

| 小测 | 固定出题设计 |
|---|---|
| `loops-q1` | total 从 0 开始，遍历 `range(1, 4)` 并累加 i，最后打印 total。正确 6；用 10、3、4 诊断右边界、只保留末项及循环次数混淆。 |
| `loops-q2` | remaining 为 3，`while remaining > 0` 每次打印 remaining。选择把递减放在循环体内、打印之后的修复，输出依次 3、2、1 后结束。其他位置要解释为何无限循环或改变结果。 |

**工作联系与独立产出**：能识别脚本中的漏处理、重复处理和死循环风险；独立统计一段状态码文本中 E 的数量，并为零次、一轮、多轮画出简短追踪。口头检查：“这个循环靠什么结束，而不是靠什么‘应该结束’？”

### 06｜列表与元组
**ID：`lists-tuples`**

**核心能力**：用合适的有序容器保存多项数据，并通过循环生成新的结果列表。

**必须讲清**：列表保留顺序、允许重复、可以修改元素或追加；`append` 修改列表，操作结果为 `None`，不能接回原变量；元组的元素位置不能重新赋值；简单拆包把固定数量的元素分别绑定给名字。无参数 `split()` 把文本按空白分成字符串列表。[P7]

**范围**：掌握列表字面量、索引、len、append、直接遍历；复用字符串切片规则。元组只掌握二元记录及拆包。用 `split()` 给后续文本处理铺路。延后推导式、列表排序、删除技巧、乘法复制、嵌套别名、星号拆包、`enumerate/zip`。不要把“元组位置不可替换”说成“里面任何对象都不能变化”。

**教学顺序与主例**：给出 `[" INFO ", "", "ERROR"]` → 复用循环逐项处理 → 把非空规范标签 append 到新列表 → 对照错误的 `result = result.append(...)` → 用 `("python", 30)` 解释固定二元记录和拆包 → 把 `"INFO  ERROR\nINFO"` 转成三个词的列表。后半只提供需要用到的最小操作。

**易误解**：列表自动去重；遍历列表得到的是下标；append 返回修改后的列表；元组与列表只是括号不同；分词需要提前逐字符写循环。

| 小测 | 固定出题设计 |
|---|---|
| `lists-tuples-q1` | 对 `["sql", "python", "sql"]` 依次遍历并打印。正确顺序仍是 sql、python、sql；考查顺序、元素与重复，不出列表方法背诵题。 |
| `lists-tuples-q2` | 已有 `items = ["python"]`，执行 `result = items.append("api")`。选择 items 变成两元素列表、result 为 None 的描述，并给出保留 items 的正确用法。 |

**工作联系与独立产出**：能读懂批量任务或记录列表处理；独立清洗一个字符串列表，保留原先顺序和重复项，输出新的非空列表。口头检查：“这个方法改变了什么，它交回了什么？”

### 07｜字典读取、更新与遍历
**ID：`dicts`**

**核心能力**：用“键对应值”的方式保存记录，或在循环中正确累计每个标签的出现次数。

**必须讲清**：字典按键访问，不按位置访问；相同键再次赋值是更新；`d[key]` 在缺键时失败，`d.get(key, default)` 可提供缺键结果，但不会插入该键；`key in d` 查的是键；直接遍历字典得到键，`items()` 可取得键值对并拆包。字典保留插入顺序，但不自动按键排序。[P5][P7]

**范围**：掌握字符串键、读取、更新、get、in、items；识别 `KeyError`，处理留到 13。示例键只用字符串，说明列表不能作键，不展开哈希与可哈希协议。暂不教 defaultdict、Counter、字典推导式、复杂嵌套和字典合并运算。

**教学顺序与主例**：从 `{"name": "python", "minutes": 30}` 读取字段 → 从空字典统计 `["python", "sql", "python"]` → 展开 `旧次数 → 加一 → 写回` → 对照只调用 get 没有更新 → 用 items 输出“标签: 次数”。

**易误解**：get 会替自己写入默认值；`in` 查字典的值；同一键会保存多份；字典的第一个键一定最小；已有键对应 None 时，get 的 default 会自动替换它。

| 小测 | 固定出题设计 |
|---|---|
| `dicts-q1` | 给定 `counts = {"python": 2}`，`n = counts.get("sql", 0)`。正确 n 为 0、`"sql" in counts` 为 False。专门诊断“读取默认值就插入键”。 |
| `dicts-q2` | 从空 counts 遍历 python、sql、python，选择 `counts[tag] = counts.get(tag, 0) + 1`；最终两键次数分别为 2、1。错误方案分别每次赋 1、缺键直接加一、只读取不写回。 |

**工作联系与独立产出**：为结构化记录、API 返回字段及频次题打基础；独立输出一组标签的计数，并解释第一次与第二次遇到相同标签时的不同状态。口头检查：“这一行是在读字典，还是已经修改了字典？”

### 08｜集合、成员判断与去重
**ID：`sets`**

**核心能力**：区分“不同项的集合”“保序去重结果”和“每项出现次数”，选择相应容器组合。

**必须讲清**：集合用于成员判断和去重，不保存可依赖的位置或显示顺序；空集合是 `set()`，不是 `{}`；添加重复元素不会增加不同项数。需要保留首次出现顺序时，由集合负责是否见过、列表负责顺序、字典负责计数。[P7]

**范围**：掌握 set、add、in/not in、len；示例成员只用规范化字符串。延后交并差全套运算、frozenset、哈希实现、复杂度和集合推导式。不用集合打印顺序作为标准答案。

**教学顺序与主例**：先看 Python、python 是否算同一项 → 明确先规范化再去重 → 集合判断是否见过 → 仅第一次加入 unique 列表 → 但每条有效记录都更新 counts → 完成 P2。把这三种状态分开解释，不直接展示三容器长程序。

**易误解**：去重后再统计还能得到原始频次；set 保留原列表顺序；原始大小写不同就一定代表不同实体；不同项数量等于有效记录数量。

| 小测 | 固定出题设计 |
|---|---|
| `sets-q1` | 给定 `{"python", "sql"}` 再 add python，正确长度仍为 2，python 仍在其中。选项不得要求判断打印时谁在前。 |
| `sets-q2` | 需求是对 python、sql、python 输出保序唯一项及原始频次。正确方案每条都计数，仅第一次见到时追加 unique；诊断“先 set 再计数”和“用集合承担顺序”。 |

**工作联系与独立产出**：能解释重复记录、唯一用户和事件次数不是同一个统计口径；独立完成 P2，并说明三个容器各保存什么。口头检查：“这一步丢掉了重复信息，后面还能恢复次数吗？”

---

## 6. M3｜写出可解释的函数

### 09｜参数与返回值
**ID：`functions`**

**核心能力**：把一段明确处理逻辑写成输入与返回值清楚的函数，并从调用处追踪到返回处。

**必须讲清**：执行 def 建立函数，普通函数体到调用时才执行；形参接收调用提供的值；return 把结果交回调用处并结束此次函数执行；print 只显示，不代替 return；没有执行带值的 return 时返回 None。每次调用有自己的局部名字，但不能据此推断传入的对象一定彼此隔离，11 会解释共享。[P6]

**范围**：掌握一个或两个参数、位置/关键字传入、接收返回值、提前返回。用既有列表和字典作为输入输出，不加类型标注。延后默认参数细节、递归、可变参数、闭包及函数装饰器。传参中的修改效应在 11 统一解释。

**教学顺序与主例**：把 P2 的标签规范化抽成 `normalize_name(text)` → 从调用处进入函数 → 接回清洗结果 → 对照只 print 的版本 → 把计数循环放入函数时检查 return 是否误缩进到循环内。

**易误解**：定义时已处理所有数据；屏幕显示结果就表示调用者得到结果；return 后的语句还会继续执行；循环内 return 等价于循环结束后的 return。

| 小测 | 固定出题设计 |
|---|---|
| `functions-q1` | 函数只 `print(text.strip())`，外部 `result = show_name(" SQL ")` 后再 `print(result)`。输出依次是 SQL 和 None；区分显示与返回，不扩展作用域陷阱。 |
| `functions-q2` | clean_names 在 for 内 append 完第一个规范名称就 return。选择把 return 移到循环结束后；对 `[" A ", "B"]` 应返回 `["a", "b"]`，空列表应返回 `[]`。 |

**工作联系与独立产出**：能够拆解 AI 生成的大段脚本并追踪数据流；独立写出并调用规范名称与批量清洗函数，处理结果可以交给下一步，而不是只能打印。口头检查：“调用者真正拿到的是什么，而不是屏幕上出现了什么？”

### 10｜作用域与默认参数
**ID：`scope-defaults`**

**核心能力**：区分函数外状态、每次调用的局部状态，以及函数定义时确定的默认值。

**必须讲清**：函数内普通赋值产生局部绑定，不能据此认为同名外部变量已更新；参数也属于此次调用的局部名字；默认值表达式在执行函数定义时求值，不在每次省略参数时重新求值；可变默认对象可能被后续调用继续使用。需要每次新建列表时，用 None 表示未提供，并在函数体内创建。[P8][P19]

**范围**：掌握局部/外部名字的最小模型、简单默认值、可变默认参数问题及 None 修复。只识别“先读后在函数内赋值”可能导致 `UnboundLocalError`，不要求背 LEGB、不讲 global/nonlocal 改写、闭包或浅拷贝。别名的完整机制留到 11。

**教学顺序与主例**：外部 `limit = 10` 与函数内 `limit = 3` → 缺省参数与显式传参 → 整数默认值在定义时确定 → 同一个默认列表被连续 append 的两次调用 → 用 None 在函数体内建立独立列表。这里用“同一默认列表”即可，不引入内存地址。

**易误解**：同名变量必定是同一份状态；默认值每次调用重新生成；把 `[]` 换成 `{}` 就能避免共享；`if not bucket` 与 `if bucket is None` 对调用者传入的空列表没有区别。

| 小测 | 固定出题设计 |
|---|---|
| `scope-defaults-q1` | 外部 limit 为 10；函数内令 limit 为 3 并 return limit。先打印调用结果，再打印外部 limit。正确 3、10；考局部重新绑定，不与默认值同时设陷阱。 |
| `scope-defaults-q2` | `add_tag(tag, bucket=[])` 追加后返回；紧接着分别打印两次省略 bucket 的调用。正确第二次累积两项。正确解释必须包含定义时建立默认对象、后续复用；修复使用 None 并在体内新建。 |

**工作联系与独立产出**：能定位“上一条任务污染下一条任务”的状态问题；独立写出多次调用互不积累的标签处理函数，并解释为何显式提供容器与省略参数是不同约定。口头检查：“这个列表是在什么时候创建的？”

### 11｜可变性、别名、浅拷贝及 is/==
**ID：`alias-copy`**

**核心能力**：在修改列表、字典或函数参数之前，判断哪些地方共享同一个对象、哪些地方只共享内部元素。

**必须讲清**：赋值绑定名字，不自动复制对象；两个名字可以指向同一可变对象；原地修改与重新绑定不同；参数得到同一对象的局部绑定，不等于自动深复制；浅拷贝只建立新外层，内部对象仍可能共享；对本课内置容器，`==` 比较内容，`is` 比较是否同一对象。[P9][P11]

**范围**：掌握直接别名、`list.copy()`/`dict.copy()`、一层嵌套的浅拷贝效应。与 10 的默认列表串联。说明元组中的可变成员仍可能被修改，但不设元组嵌套小测。延后 deepcopy 用法、垃圾回收、引用计数、小整数缓存、字符串驻留、`id()` 输出及复杂图结构。

**教学顺序与主例**：同一记录给两个名字 → 改字段与重新赋一个新字典的区别 → 函数内 append 为什么影响调用者 → 复制 `[[1], [2]]` 的外层 → 改内部列表与追加外层元素的不同影响 → 回到 P3，规定清洗函数主动构造新记录。

**易误解**：换名字就是复制；传进函数后自动隔离；copy 等于所有层完全独立；内容相等就是同一个对象；修复副作用的唯一办法是任何地方都 deepcopy。

| 小测 | 固定出题设计 |
|---|---|
| `alias-copy-q1` | `a = ["sql"]`、`b = a`、`b.append("api")`。正确 a 与 b 内容都改变，`a is b` 为 True。干扰项区分别名、自动复制和内容/身份混淆。 |
| `alias-copy-q2` | `a = [[1], [2]]`，`b = a.copy()`，再 `b[0].append(9)`。正确 a 也成为 `[[1, 9], [2]]`，但 `a is b` 为 False。解析明确“外层不同、第一项共享”，不以报对象地址作答案。 |

**工作联系与独立产出**：能审查数据清洗函数是否修改了输入；独立构造新的记录字典返回，并用两次调用和前后输入对照确认没有污染原记录。口头检查：“我复制的是哪一层，这次实际改的是哪一层？”

### 12｜简单推导式与排序 key
**ID：`comprehensions-sort`**

**核心能力**：在不隐藏处理规则的前提下，用简单转换表达式和明确排序键整理记录。

**必须讲清**：一层列表推导式可以展开成已经会写的 for/if/append；sorted 返回新列表，list.sort 原地排序并返回 None；key 提供“从每项提取比较依据”的函数，要传函数本身而不是先调用它；相同排序键保留原相对顺序。[P7][P10]

**范围**：掌握单层列表推导式、至多一个过滤条件、sorted 的 key 与 reverse。list.sort 只作返回值对照。延后 lambda、嵌套/字典/集合推导式、生成器表达式、复合键、多轮排序、比较器和排序算法。转换需要多个步骤时明确保留普通循环。

**教学顺序与主例**：把一个已完成的列表清洗循环改写成单层推导式 → 能再改回循环 → 对分钟数排序 → 定义 `minutes_key(record)` → 对照 `key=minutes_key` 与错误的立即调用 → 用两条同分钟记录解释稳定性 → 完成 P3。

**易误解**：推导式越短越高级；使用 sorted 已把原列表排好了；key 是比较两个对象的函数；返回相同键的项会随机重排；换成 list.sort 后仍能把返回值当列表。

| 小测 | 固定出题设计 |
|---|---|
| `comprehensions-sort-q1` | 对 `[" A ", "", "B"]`，给出 `[x.strip().lower() for x in raw if x.strip()]`，选择等价的普通循环，结果 `["a", "b"]`。必须解释过滤发生在追加之前，而不是只背结果。 |
| `comprehensions-sort-q2` | 提供依次为 A:30、B:10、C:10 的记录，以及返回 minutes 的命名函数。选用 `sorted(records, key=minutes_key)`；顺序 B、C、A，原列表仍 A、B、C。必须解释 key 与同键保序。 |

**工作联系与独立产出**：能读懂数据转换和排名代码，并拒绝不必要的一行炫技；独立完成 P3，清楚解释清洗、过滤、排序分别在哪一步发生。口头检查：“这里传给 key 的是处理规则，还是已经算出来的值？”


## 7. M4｜从片段走向脚本

### 13｜读 traceback 与处理异常
**ID：`exceptions`**

**核心能力**：根据错误信息找到失败操作，并把“预期的输入问题”与“需要修复的程序错误”分开。

**必须讲清**：traceback 展示调用路径，末尾给出异常类型和说明；定位时先看类型，再找自己代码中实际失败的行，不能只看最上面的调用行；异常发生后 try 内剩余语句不继续执行，匹配的 except 才处理；只捕获已决定如何处理的异常。[P12]

**范围**：掌握 `try/except ValueError`、`except ... as exc` 的最小读法、局部处理范围。结合旧例识别 NameError、TypeError、KeyError；FileNotFoundError 在下一课具体使用。延后自定义异常、主动 raise 的设计、异常链、完整异常继承树、finally/else 和重试框架。

**教学顺序与主例**：重新看 02 的 `int("bad")` → 从调用行找到转换失败行 → 逐项转换 `["0", "bad", "5"]` → 明确规则“非法项记录原文本并跳过，合法零保留” → 只包住转换操作 → 对照吞掉所有异常或把整个循环包在一次 try 中的结果。

**易误解**：错误在谁调用函数就一定是谁写错；捕获异常后从失败行继续；try 越大越安全；所有失败统一返回 0 就方便；`except: pass` 等于解决了问题。

| 小测 | 固定出题设计 |
|---|---|
| `exceptions-q1` | 提供一个调用 parse_count 后在函数内 `int(raw)` 失败的短 traceback，raw 为 `"bad"`。正确定位转换行和 ValueError，而不是外层 print、文件名或“Python 没装好”。 |
| `exceptions-q2` | 对 `["0", "bad", "5"]`，选择逐项仅捕获转换 ValueError 的实现。有效结果应为 `[0, 5]`，非法项仅为 `"bad"`。干扰项把 bad 变 0、遇错后漏掉 5、把合法 0 当失败。 |

**工作联系与独立产出**：能带着定位结论向 ChatGPT 求助，而不是只贴整段报错；独立修复一个转换脚本，并说明遇到预期错误后究竟跳过了什么。口头检查：“这是输入不符合约定，还是程序本身写错了？”

### 14｜pathlib、UTF-8 文本及 with
**ID：`paths-text`**

**核心能力**：知道文件从哪里读取、以什么编码解释、何时关闭，以及何时可以安全开始写报告。

**必须讲清**：Path 表示路径，不是文件内容；相对路径相对于进程当前工作目录，不自动相对于脚本文件；`Path.cwd()` 可辅助定位；显式 UTF-8 读写；`with` 管理文件使用范围；成功以 `w` 打开已有文件就会截断内容，不能等到第一次 write 才考虑覆盖风险。[P13][P14]

**范围**：掌握最小 Path 导入、构造及路径拼接、`Path.open`、`read`、`write`、with、`"\n".join(字符串列表)`。识别 `\n`、FileNotFoundError、UnicodeDecodeError 及文件操作中的 OSError。延后目录遍历、大文件流式处理、二进制操作、编码探测、自动创建目录、原子替换和上下文管理器实现。

**教学顺序与主例**：在练习目录给出 input.txt → 先打印工作目录核对位置 → 用 with 和 UTF-8 读取内容 → 接上已学 lower、split、列表和计数 → 用 join 形成报告文本 → 读取和处理成功之后才用 w 写独立的 report.txt。每个阶段单独展示，不一次引入完整词频程序。

**易误解**：脚本在哪文件就一定从哪里找；Path 对象就是已读取文本；read 自动得到词列表；文件不存在就等于空文件；w 只往末尾追加；字符能显示就说明任何编码都可以。

| 小测 | 固定出题设计 |
|---|---|
| `paths-text-q1` | 脚本位于项目目录，但当前工作目录设为它的上一级；代码使用 `Path("input.txt")`。正确先核对工作目录与文件位置，不能断言必然在脚本旁查找，也不能把路径错误当编码错误。 |
| `paths-text-q2` | 已存在 report.txt，要求输入文件读取失败时保留旧报告。选出先成功读取并生成报告字符串，再打开输出文件 w 写入的方案；解释为何先打开输出再读取会过早截断。 |

**工作联系与独立产出**：能处理“我电脑上能跑，换目录就找不到”的问题，避免覆盖输入文件；独立读取一个 UTF-8 练习文件并写到另一文件，处理缺失输入且不谎报成功。口头检查：“当前这一步只是表示路径，还是已经触碰了磁盘内容？”

### 15｜模块、入口、虚拟环境与安装依赖
**ID：`modules-venv`**

**核心能力**：让处理函数可被别处复用，同时明确这个项目究竟由哪个 Python 解释器运行。

**必须讲清**：模块可以包含函数，也可以包含会在正常导入时执行的顶层语句；把执行流程放进 main，再用 `if __name__ == "__main__":` 区分直接运行与被导入；标准库、本地模块、第三方包不是一回事；虚拟环境隔离项目依赖，安装到别的解释器不会自动让当前解释器获得该包。[P15][P16]

**范围**：掌握两个 `.py` 文件间的普通导入、main 入口保护、查看解释器路径、创建/选用一个项目环境的最短流程；识别 `python -m venv .venv` 和“指定解释器 -m pip”的含义。V1 实践不要求安装第三方包。延后包发布、相对导入、导入缓存细节、依赖管理工具比较、锁文件策略和环境管理大全。

**教学顺序与主例**：把已有清洗/计数函数放入 word_tools.py → runner.py 导入调用 → 对照“导入即生成报告”的副作用 → 加入口保护 → 查看 `sys.executable`[P17]，核对 PyCharm 的实际解释器 → 说明安装命令为何也要指向它。正式教材给出 Windows 与 macOS/Linux 的解释器路径示意，但不把模板路径冒充用户真实路径。

**易误解**：import 只读取函数声明、不执行任何顶层代码；入口保护会阻止别的文件导入函数；系统终端能 import，就说明 PyCharm 使用的解释器也能；激活某个终端环境等于所有运行入口已切换。

| 小测 | 固定出题设计 |
|---|---|
| `modules-venv-q1` | word_tools.py 顶层打印 `"loaded"`，main 内打印 `"report"`，main 受入口保护。另一个脚本首次正常 import 它；正确只显示 loaded。解释保护仅控制受保护入口，不屏蔽其他顶层语句。 |
| `modules-venv-q2` | 已确认 PyCharm 使用项目 .venv，而第三方包装在系统 Python。正确选择使用项目 .venv 的解释器执行 `-m pip install 包名` 并核对路径；不是盲目反复运行系统 pip、修改业务代码或安装到所有环境。题目为环境判断，不要求联网安装。 |

**工作联系与独立产出**：能把一次性脚本整理为可复用代码，定位基础的 ModuleNotFoundError 环境问题；独立建立可导入的工具模块和明确入口，确认导入工具模块不会写报告。口头检查：“我安装包所用的 Python，与实际运行脚本的是不是同一个？”

### 16｜断言、边界用例与可测试函数
**ID：`tests-boundaries`**

**核心能力**：先写清正确结果，再用正常与边界样本检验函数，避免只凭一次运行成功认定完成。

**必须讲清**：测试由输入、预期结果和实际结果比较构成；预期应来自需求的独立推演，不是复制待测实现再算一遍；assert 失败表示约定没有满足；assert 可在优化模式下被移除，不能代替运行时输入验证；逻辑函数与文件读写分离后更容易独立检查。[P1]

**范围**：掌握简单 assert、正常/空/单项/重复/零值/失败路径的选择，以及不修改输入、连续调用不串状态的检查。延后 pytest、mock、覆盖率指标、属性测试、基准测试与持续集成工具教学。测试不能只断言“没有抛错”或“结果不是 None”。

**教学顺序与主例**：回看 P3 清洗规则“零分钟合法” → 展示正常样本通过但零值被误删的实现 → 选择能暴露问题的测试 → 用预先手算的词频写断言 → 将纯处理逻辑与文件连接分别验证 → 完成 P4。

**易误解**：例子跑通就意味着所有输入正确；把函数自己算出的结果当预期；assert 是必须保留的用户输入校验；测试失败应该删测试；对报告只检查包含某词，不检查次数、顺序和完整文本。

| 小测 | 固定出题设计 |
|---|---|
| `tests-boundaries-q1` | P3 约定 minutes 为 0 的非空名称记录要保留，错误实现却要求 `minutes > 0`。选择含 `{"name": "CLI", "minutes": 0}` 且预期保留的测试；普通正数和单独空列表测试不足以揭示这个错误。 |
| `tests-boundaries-q2` | 对 `"Python python"` 的词频函数，选择手工写定 `{"python": 2}` 作为完整预期的断言。错误方案只打印、仅检查一个键、或把同一待测函数再次调用的结果作为 expected。 |

**工作联系与独立产出**：能够向面试官或同事解释正常结果、边界和失败策略；独立完成 P4，并用一组小测试证明关键行为，而不是用“代码是 AI 生成的”替代验证。口头检查：“这个测试要抓哪一种错误，错了时它真的会失败吗？”


## 8. 四个模块实践：固定任务与验收样例

四项实践不是互不相干的项目。P1 建立“输入—规则—输出”；P2 将它扩展为逐条处理和维护状态；P3 将处理逻辑拆成输入输出清楚的函数；P4 沿用计数与排序，补上文件、模块、异常和测试。

每项正式实践按“任务 → 数据与规则 → 输出 → 分级提示 → 参考解法 → 自查依据”编写。提示先解释怎么拆任务，再指出相关旧课，最后才接近实现。参考解法只能使用截至该模块已教过的能力，不得比正文突然高一个技术层级。

### P1｜生成资料费用说明
**附在 `conditions`；复用 01—04。**

**任务**：从给定变量生成三行费用说明。数据写在脚本开头，不要求交互输入、批量处理、函数或文件读写。

固定普通样例：

| 输入名 | 值 | 约束 |
|---|---|---|
| raw_title | `" Python Basics "` | 字符串，可能为空白 |
| raw_count | `"3"` | 保证是可由 int 转换的整数文本，可以为零或负数 |
| unit_price | `20` | 固定整数元单价，本实践不修改 |
| is_member | `False` | 真正的 bool，不使用字符串 `"False"` |

**规则锁定**：名称去两端空白并转小写；数量转整数。规范名称为空或数量小于 0 时，只输出“输入无效”。其余情况先计算资料费用；零份不收任何费用；正数量且资料费用达到 100 元，或者是会员，服务费为 0；其他情况服务费为 8 元。金额只用小整数元计算，`.2f` 仅用于输出显示，本实践不讲真实货币精度方案。

普通样例的完整输出：

```text
资料：python basics
份数：3
应付：68.00 元
```

**固定验收**：

| 相对普通样例的变化 | 必须得到的结果 |
|---|---|
| 无变化 | 应付 68.00 元 |
| raw_count 改为 `"4"` | 应付 88.00 元，未达到免服务费阈值 |
| raw_count 改为 `"5"` | 应付 100.00 元，边界值免服务费 |
| is_member 改为 True | 应付 60.00 元 |
| raw_count 改为 `"0"` | 仍为合法说明，应付 0.00 元，不加 8 元 |
| raw_count 改为 `"-1"` | 只输出“输入无效” |
| raw_title 改为全空格 | 只输出“输入无效” |

**允许的提示顺序**：先列规范化后的变量 → 画出无效、零份、普通费用三条路径 → 再细分免服务费条件 → 最后格式化。参考解法使用清楚的 if/elif/else，不使用三元表达式或把布尔值乘进金额。

**如何复用**：01 保证变量计算顺序；02 转换数量；03 规范文本与格式化；04 决定路径和阈值。学习者应能不看参考解法写完，并解释只修改数量后必须重新计算哪些中间值。

**迁移检查**：把免服务费阈值从 100 改成 80，只修改一处规则，解释哪一条验收结果随之改变。不要增加“支持任意坏输入”或真正收款功能。

### P2｜标签去重与频次统计
**附在 `sets`；复用 01—08，重点连接 03、05—08。**

**任务**：处理给定的文本标签记录，输出有效数量、不同标签数量、忽略数量，以及每个标签的原始频次。输入是列表，不读文件，不定义函数。

固定输入：

```python
raw_tags = [
    " Python ", "SQL", "python", "",
    "API", " sql ", "   ", "API",
]
```

**规则锁定**：先 strip 再 lower；规范化后空文本忽略，其余每条都计数。唯一标签按首次有效出现顺序输出，不能按字母排序、不能依赖集合遍历顺序。重复记录不从频次中删除。

普通样例的完整输出：

```text
有效记录：6
不同标签：3
忽略记录：2
python: 2
sql: 2
api: 2
```

**中间结果约定**：seen 表示已经出现过的规范标签；unique 保存首次出现顺序；counts 保存每个标签的次数。三者职责必须在提示与解析中说清。不可用 Counter、sorted、推导式或函数封装替代本模块训练。

**固定验收**：

| 输入 | 必须得到的结果 |
|---|---|
| 普通样例 | 有效 6、不同 3、忽略 2，顺序 python/sql/api，各 2 次 |
| `[]` | 三项数量均为 0，无标签明细 |
| `["", "   "]` | 有效 0、不同 0、忽略 2 |
| `["API", " api ", "api"]` | 有效 3、不同 1、api 次数 3 |
| `["SQL", "Python"]` | 明细顺序 sql、python，不重新排序 |

**允许的提示顺序**：先只输出每条清洗结果 → 再加入有效/忽略计数 → 再复用 07 的字典频次 → 最后加入 seen 与 unique 保序去重。参考解法中，更新 counts 不能放在“首次见到”分支内。

**如何复用**：将 P1 的一次规范化与判断放进 05 的循环；06 用列表保存输出顺序；07 维护计数；08 分离唯一性与频次。学习者应能口头追踪第三条 `"python"`：有效数增加，次数增加，但 unique 不增加。

**迁移检查**：在末尾再加一个 `" SQL "`，指出哪些数量改变、输出顺序为何不变。不能把新的统计口径临时改成“去重后的数量就是总次数”。

### P3｜不修改输入的记录清洗与排序
**附在 `comprehensions-sort`；复用 03—12，重点连接 09—12。**

**任务**：把记录规范化、过滤无效记录，再按 minutes 升序输出。处理过程必须拆成命名清楚的小函数，不能修改输入列表或其中原始字典。

固定输入：

```python
records = [
    {"name": " Python ", "minutes": 30},
    {"name": "", "minutes": 10},
    {"name": "SQL", "minutes": 20},
    {"name": "python", "minutes": 15},
    {"name": "API", "minutes": 20},
    {"name": "CLI", "minutes": 0},
    {"name": "BAD", "minutes": -1},
]
```

**输入约束与规则锁定**：每条都有 name 与 minutes；name 是字符串，minutes 是整数。名称 strip 后 lower；规范名称为空或 minutes 为负时忽略；**零分钟合法**。不同记录即使同名也保留，不合并、不去重。按 minutes 升序，分钟数相同保持原相对顺序。缺键、错误类型及非法数字文本不在本实践处理范围。

**函数职责固定**：

| 函数 | 约定 |
|---|---|
| `normalize_name(text)` | 返回规范名称字符串 |
| `clean_record(record)` | 返回新建的有效记录字典；无效时返回 None；不修改原记录 |
| `clean_records(records)` | 逐条调用 clean_record，收集非 None 结果并返回新列表；空输入返回空列表 |
| `minutes_key(record)` | 返回用于排序的分钟数 |

顶层代码串联清洗、sorted 和输出。clean_records 每条只调用一次 clean_record，用普通循环保存中间结果；不能为了强行使用推导式而重复调用。推导式的掌握由 12 的独立任务与小测检查，不要求所有实践都使用它。

固定输出：

```text
有效记录：5
cli: 0
python: 15
sql: 20
api: 20
python: 30
```

**固定验收**：

| 检查 | 必须得到的结果 |
|---|---|
| 普通样例 | 输出如上，sql 在 api 前，两个 python 记录均保留 |
| 空输入 | 返回空列表，输出“有效记录：0” |
| 单条 name 非空、minutes 为 0 | 该记录保留 |
| 全部名称为空白或分钟数为负 | 无有效记录 |
| 处理前后查看原 records | 原列表顺序、原 name 中的空格/大小写、各字段值均不变 |
| 对同一输入连续运行两次清洗 | 得到内容相同的结果，没有第二次额外累积 |
| 修改一个清洗结果字典的 name | 对应原始记录的 name 不随之改变 |

最后一项针对本任务只有字符串和整数的平面记录；不得把这个结论泛化为“所有深层嵌套都被复制了”。

**允许的提示顺序**：先写单条函数并试有效/无效/零值 → 再组合成列表处理 → 最后定义排序 key → 分别核对输入、清洗结果、排序结果。参考解法按函数拆块解释，而不是先贴长程序。

**如何复用**：03 的规范化进入函数；06/07 提供列表与记录字典；09 确定返回契约；10 防止状态跨调用积累；11 解释新建结果与原地修改；12 决定排序与同键顺序。与 P2 的关键区别必须讲明：这次同名记录是不同条目，不能习惯性去重。

**迁移检查**：需求变为降序时只改变排序配置；同分钟记录仍应保序。另问：“改成排序后直接修改原字典，会破坏哪条约定？”不把新增字段、深层嵌套或脏类型加入必做范围。

### P4｜读取文本，生成可验证的词频报告
**附在 `tests-boundaries`；综合 01—16，重点连接 07、12—16。**

**任务**：在专门的 PyCharm 练习目录读取 input.txt，生成 report.txt；计数与报告格式化可独立导入、独立测试。仅处理教学用小文件，可一次读取全文，不增加性能优化。

**“词”的定义锁定**：对文本先 lower，再用无参数 split 按空白切分。连续空白不产生额外词项。标点不删除；`"python,"` 与 `"python"` 是不同词。不做中文分词；没有空白的一段中文作为一个词项。规则必须写进题目，不能让内容 Agent 自行“改进”。[P5]

**报告规则锁定**：显示总词数和不同词数；词条按次数降序；次数相同按首次出现顺序。复用字典插入顺序与稳定排序，key 只返回次数，不临时引入复合键。输出为 UTF-8 文本，行间一个换行，最后也保留换行。空文件产生合法的两行零值报告，而不是报错。

固定 input.txt 内容；第三行为空行：

```text
Python python
SQL api SQL

API python
```

固定 report.txt：

```text
总词数：7
不同词数：3
python: 3
sql: 2
api: 2
```

**代码职责固定，本文不提供完整实现**：

| 文件/函数 | 职责与返回约定 |
|---|---|
| word_tools.py：`count_words(text)` | 接收字符串，返回词频字典；无有效词时返回 `{}`；不读写文件、不打印 |
| word_tools.py：`count_key(item)` | 接收 items 产生的二元项，返回次数，用于 sorted |
| word_tools.py：`format_report(counts)` | 返回完整报告字符串，不修改 counts；用循环累计总数，再构造各行并 join |
| main.py：`main()` | 路径、读取、调用处理、写入、预期错误提示；入口保护下运行 |
| check_examples.py | 导入纯函数，使用明确输入与手工预期做 assert；不依赖自动测试框架 |

main.py 只在读取、计数和格式化成功后打开输出文件。使用单独的读写错误处理，不把所有处理函数包进一个吞掉异常的大 try。工具模块被导入时不打印、不生成报告。

**失败策略固定**：

| 情况 | 必须如何处理 |
|---|---|
| input.txt 不存在 | 明确提示输入不存在；不生成成功提示；保留已有 report.txt |
| 不是有效 UTF-8 | 明确提示编码问题；不能静默忽略坏字符；保留已有报告 |
| 其他预期读文件错误 | 报告读取失败，不开始写输出 |
| 写 report.txt 失败 | 明确提示写入失败，不报成功；V1 不承诺原子写或旧报告完整保留 |
| 函数内部出现 NameError、逻辑性 TypeError 等 | 不伪装成空文件或零词报告，应暴露并修复程序错误 |

FileNotFoundError 属于文件操作异常；若同一捕获链同时覆盖它和 OSError，具体情况放在通用情况之前。文件异常用于环境失败，不能吞掉算法错误。[P12][P18]

**固定内容测试**：

| 样本或检查 | 独立预期 |
|---|---|
| 普通样例 | 词频 python=3、sql=2、api=2；完整报告与上例一致 |
| 空文本 `""` | `"总词数：0\n不同词数：0\n"` |
| 仅空白 `" \n\t "` | 与空文本同样的零值报告 |
| 单词 `"Python"` | python=1；总数 1、不同词数 1 |
| `"API api API"` | api=3；只有一个明细词条 |
| `"sql api"` | 两词各 1；明细顺序 sql、api，不能按字母排序 |
| `"数据 data 数据"` | 数据=2、data=1；UTF-8 正常显示；总数 3 |
| `"Python, python"` | python,=1、python=1；两词不同，不擅自删除逗号 |
| 同一输入连续调用 count_words 两次 | 结果相同，不跨调用累积 |
| format_report 前后比较 counts | 内容及顺序不被修改 |
| 直接 import word_tools | 没有控制台输出，不生成或修改报告等业务文件 |
| 运行入口两次 | 报告相同，不把新报告追加在旧报告后 |

此外，在**专用测试目录**验证输入缺失、无效 UTF-8、写入失败三条文件路径。内容 Agent 提供所需错误样本及操作说明；无效 UTF-8 样本可由 Agent 预制，写入失败可通过把 report.txt 预先建成目录来模拟。学习者不必为制造样本提前学习二进制写入、权限 API 或 mock。

**允许的提示顺序**：先脱离磁盘写 count_words，并复用 P2 → 用固定字典写 format_report，复用 12 的排序与 14 的 join → 两个函数正确后才接读取 → 加写入与针对性错误处理 → 拆模块和入口 → 运行断言与文件样例。参考解法与解释必须保持这一顺序，不能先交一个全功能程序让学习者抄写。

**如何复用**：03/06 完成文本规范化和 split；05/07 累计词频；09/10 保持函数契约和每次调用的独立状态；11 避免副作用；12 形成稳定顺序；13/14 管理文件失败；15 支持导入和明确运行；16 用需求校验结果。

**独立完成的证据**：能从空文件搭出核心函数，解释同频排序、空文件与缺失文件的区别，修改一条规则后指出应更新哪些测试。完成参考解法复现后，再用新文本手算一次预期并运行核对；无需额外网络请求或外部库。


## 9. 正式教材编写时不得自行改变的决定

### 9.1 每课的交付检查

每课必须能回答同一组问题：主目标是什么；主例依赖哪些旧课；本课新引入哪个机制；两道题分别诊断什么；学习者离开页面后要独立做什么。不能只有“了解、熟悉、掌握”而没有可观察行为。

正文里的每个新操作，必须在本课先解释、或已在前课出现；只允许第 2.2 节列明的最小桥接，不允许用“后面会讲”掩盖看不懂的核心代码。尤其检查 `split`、import、key 函数、join、None 与可变对象的出现位置。

“易误解”不是集中堆在末尾的考试口诀。应选择最影响本课目标的一项，紧跟对应主例做一个对照；其他误解进入选项解析或一句提醒。一个例子原则上只改变一个条件，让学习者能归因。

### 9.2 小测的写作要求

保留本文的 32 个题目 ID、目标和判定锚点。可以调整变量名、选项顺序和自然语言表达，不能改测未教语法、冷僻边界或 Python 实现细节。每题应有完整前提和唯一正确选项；若题目问执行结果，要说清打印、返回值还是变量内容。

解析必须写出正确结果的形成过程，并逐项指出错选项对应的错误理解。不能用“Python 就是这样规定的”“注意细节”代替机制说明；不能把语法复杂程度当成答案优劣依据。

本蓝图中的额外验收样例用于检查练习与参考解法，不增加网页小测数量。环境题和方案选择题不能假装通过运行一句表达式就验证了全部正确性，需要结合题目声明的环境或规则审核。

### 9.3 参考解法的约束

优先可解释、可追踪的直接写法。变量名体现作用，保留必要中间结果；不使用未讲的内置函数或库把目标过程省掉。P1/P2 不提前封装函数；P3 不为代码更短滥用推导式；P4 不添加第三方包、网络服务、分词或额外文件管理功能。

参考解法先解释数据怎样流动，再解释代码。它不是唯一合法答案：学习者的实现只要使用当前知识、满足约定、能解释机制，就可接受。不能把照抄参考变量名当作学会的证据。

### 9.4 示例与答案核验

正式 Agent 必须在 Python 3.13 下验证所有完整示例、正确选项对应代码及四项参考解法；故意报错时核对异常类别与失败操作，不固定不同环境中的完整路径和行号。依赖前文的片段标注所需上下文，不能声称复制孤立片段就能运行。

输出里的顺序必须有来源：列表原顺序、字典首次插入顺序或明确定义的稳定排序。集合显示顺序、对象地址、终端工作目录、系统解释器路径不得被写成通用固定答案。

P1—P4 的输入约束、普通样例与边界样例必须与题目和解法一致。优先发现“说明里说保留零、代码却按真假过滤”“说明里说不修改输入、结果却共享原字典”等契约冲突。

## 10. 内容验收与学习验收

### 10.1 内容验收

| 检查面 | 通过标准 |
|---|---|
| 范围与数量 | 16 个原课程 ID、原顺序、32 道正式题、四个指定课末实践；没有扩展课程混入 |
| 递进 | 05 不依赖未教列表，03 不偷教 split；默认参数、别名、文件导入和排序有明确桥接 |
| 可读性 | 主例短、一次变化少；长实践按阶段或函数解释；不是术语目录或 API 速查表 |
| 机制 | 每课有至少一个“为什么会这样”的解释和可验证对照 |
| 小测 | 场景符合本蓝图；正确项唯一；错误选项有具体误解依据 |
| 实践 | 提供固定输入、规则、输出、提示、解法和边界；参考解法不使用超前能力 |
| 正确性 | 输出、异常类型、顺序及副作用经过实际检查；路径与环境假设明确 |
| 长期衔接 | M2 的计数、M3 的函数契约、M4 的文件与测试能连起来，不是四套互相无关模板 |

### 10.2 学习者的四次能力检查

M1 后：看一段短脚本，先解释每个变量的当前含义，再修改一个条件并预测结果。  
M2 后：对一批记录，说明清洗、过滤、计数和去重分别保留或丢失什么信息。  
M3 后：不看参考解法写出小函数，解释返回值、共享状态、输入是否被修改及排序规则。  
M4 后：从一个明确需求完成小脚本，能定位一次失败，并用正常与边界测试证明关键行为。

这些是自我检查与答疑依据，不是新增考试、自动评分或阻止继续学习的门槛。两道选择题答对不能单独证明已经会独立写程序。

**最终能力标准**：面对一段 AI 给出的 Python 代码，学习者不再只能“运行看看”，而能先说出它的输入、状态变化、预期输出、可能失败的位置，以及自己准备怎样验证它。

## 11. 事实依据与核验边界

教学顺序、样例数据、练习规则和难度取舍是本项目的设计决定；下列官方资料仅用于核对 Python 语义。它们不是要求学习者额外通读的教材。

[B1] 配套开发规格 `python_relearn_kindle_v1_spec.md`，第 5 节课程规划与写作标准、第 6 节教材格式。课程 ID、顺序、数量及首发边界沿用该文件。

| 标记 | 官方资料与对应范围 |
|---|---|
| P1 | Python 3.13，Simple statements：赋值、return、assert。`https://docs.python.org/3.13/reference/simple_stmts.html` |
| P2 | Python 3.13，Built-in Functions：type、int、str、print。`https://docs.python.org/3.13/library/functions.html` |
| P3 | Python 3.13，Built-in Constants：None、True、False。`https://docs.python.org/3.13/library/constants.html` |
| P4 | Python 3.13 Tutorial，An Informal Introduction：字符串、索引、切片。`https://docs.python.org/3.13/tutorial/introduction.html` |
| P5 | Python 3.13，Built-in Types：真值、布尔运算、字符串方法、字典顺序。`https://docs.python.org/3.13/library/stdtypes.html` |
| P6 | Python 3.13 Tutorial，More Control Flow Tools：循环、函数与参数。`https://docs.python.org/3.13/tutorial/controlflow.html` |
| P7 | Python 3.13 Tutorial，Data Structures：列表、元组、字典、集合、推导式。`https://docs.python.org/3.13/tutorial/datastructures.html` |
| P8 | Python 3.13，Execution model：名字绑定与作用域。`https://docs.python.org/3.13/reference/executionmodel.html` |
| P9 | Python 3.13，copy：绑定与浅拷贝。`https://docs.python.org/3.13/library/copy.html` |
| P10 | Python 3.13，Sorting Techniques：key、返回值与稳定排序。`https://docs.python.org/3.13/howto/sorting.html` |
| P11 | Python 3.13，Expressions：比较与身份判断。`https://docs.python.org/3.13/reference/expressions.html` |
| P12 | Python 3.13 Tutorial，Errors and Exceptions：traceback 与针对性处理。`https://docs.python.org/3.13/tutorial/errors.html` |
| P13 | Python 3.13 Tutorial，Input and Output：文本读写、编码与 with。`https://docs.python.org/3.13/tutorial/inputoutput.html` |
| P14 | Python 3.13，pathlib：Path、工作目录与文件操作。`https://docs.python.org/3.13/library/pathlib.html` |
| P15 | Python 3.13 Tutorial，Modules：导入、模块顶层执行与脚本入口。`https://docs.python.org/3.13/tutorial/modules.html` |
| P16 | Python 3.13 Tutorial，Virtual Environments and Packages：解释器环境与包安装。`https://docs.python.org/3.13/tutorial/venv.html` |
| P17 | Python 3.13，sys：sys.executable。`https://docs.python.org/3.13/library/sys.html` |
| P18 | Python 3.13，Built-in Exceptions：文件和解码异常的类别。`https://docs.python.org/3.13/library/exceptions.html` |
| P19 | Python 3.13，Compound statements：函数定义及默认参数求值。`https://docs.python.org/3.13/reference/compound_stmts.html` |

资料核对日期：2026-09-23。蓝图校核使用 Python 3.13.5，已通过 67 项具体场景检查，覆盖主要语义示例、四项实践的固定结果、状态隔离及文件失败行为。这只是蓝图中的样例与规则校核；正式课文、完整选项及正式参考解法仍须在编写后分别测试，不能沿用此结果宣称尚不存在的教材已经验收。

