# Python 重学网页｜V1 开发规格

版本：1.0 · 2026-09-23  
交付对象：Muse Code / Coding Agent  
交付性质：开发规格，不包含网页实现。文中的“必须”属于验收要求，“后续”不属于 V1。

## 1. 产品定位与关键决策

这是一个以 Kindle Scribe 浏览器为主要使用场景的 **Python 微课阅读、自测与复习工具**，不是在线编程平台。面向有数学和科研经验、用过 Python，但需要重新建立独立读代码、解释代码和编写小程序能力的学习者。

分工固定：网页负责短篇讲解、阅读代码、预测结果、自测和记录；ChatGPT 负责进一步讲解与答疑；PyCharm 负责实际编写、运行和调试代码。网页不接入 ChatGPT API，也不运行 Python。

**技术定案：构建时生成静态 HTML，多页面导航，每课内部以少量原生 JavaScript 实现分步阅读，localStorage 保存本地状态，部署到 GitHub Pages。** GitHub Pages 支持静态 HTML/CSS/JavaScript 及构建后的站点，本方案不需要服务器端程序。[R2]

Amazon 的 Scribe 用户指南说明浏览器支持 JavaScript，且允许用户禁用 JavaScript；这不足以证明某台设备支持全部现代 Web API。因此采用“先保证静态阅读，再增强交互”，不假设特定 Chromium/WebKit 版本，也不把 Fire 平板的 Silk 文档当作 Scribe 兼容证明。[R1]

## 2. V1 边界

### 2.1 必须交付

交付首页、课程目录、微课页、错题与复习页、设置页；支持分步阅读、展开解释、单选自测、答案解析、显式标记已读、PyCharm 实践自报状态、断点继续、本地保存和文本备份恢复。

**首发教材：16 节完整基础微课，每课 2 道题，共 32 道题；每个模块末尾附 1 项 PyCharm 实践，共 4 项。** 每课一般 6—8 步。不得用只有标题、占位文案或重复模板的页面充数。

功能闭环在 V1 完成；数据处理、API、自动化与更多面试内容在后续沿用相同格式添加。V1 不声称已经完整覆盖这些岗位能力。

### 2.2 明确不做

不做账号、云同步、数据库、在线 IDE、Python/WASM 运行环境、AI 聊天、后台 CMS、全文搜索、笔记编辑器、打卡积分、排行榜、自适应推荐或间隔重复算法。不做视频、动画、复杂图表、PWA、Service Worker 或全站离线包。

不增加框架只是为了日后“可能用到”。不要改成 React/Vue 单页应用；不要加入路由兜底重写、组件库或客户端 Markdown 解析器。

## 3. 页面与导航

| 页面 | 地址（相对于站点根目录） | 内容与操作 |
|---|---|---|
| 首页 | `index.html` | 首次显示“开始学习”；以后显示“继续学习”、已读课数、待复习题数；提供目录、复习、设置入口 |
| 目录 | `course.html` | 按模块列出全部已发布课程；显示未开始/学习中/已读；模块末尾显示实践状态和入口 |
| 微课 | `lessons/<lessonId>.html` | 课程标题、当前步数、当前内容、上一部分/下一部分；末步提供标记已读和下一课 |
| 复习 | `review.html` | 只列待复习题，按课程顺序排列，每页最多 10 条；进入原课程中的对应题目重做 |
| 设置 | `settings.html` | 三档字号、本地保存状态、导出/导入备份、清除本课程学习记录 |

导航使用真实链接。站内链接同标签打开，不依赖悬浮菜单、汉堡菜单、手势或弹窗。

首页继续规则：最近所在课未读完时继续其所在步骤；否则进入目录中第一节未读完的课；全部已读后显示“重新查看课程”和复习入口。不得进入首页就自动跳转。

每步使用稳定锚点，例如 `lessons/dicts.html#s-quiz-1`。打开指定锚点时优先定位该步；没有锚点才恢复本课保存的步骤；没有记录或步骤已删除时从第一步开始。课程地址可直接打开、刷新和加入书签，不依赖先访问首页。

## 4. 核心学习交互

### 4.1 分步阅读

每课的完整正文、示例、题目与解析均在首次 HTML 响应中，不按步请求服务器。JavaScript 正常时一次只展示一步；“上一部分/下一部分”即时切换、更新锚点并保存步骤，不做动画。切换后把焦点移到新标题，滚动到正文起点；浏览器后退/前进能够恢复步骤。初始化恢复位置不得额外堆积历史记录。

默认学习顺序为：**学习目标与核心解释 → 示例及结果展开 → 常见误区 → 小测 1 → 小测 2 → 回忆总结**。模块最后一课再增加实践步骤。允许自由前后阅读，不锁课程、不要求测验全对才能继续。

展开以一个完整的解释块或输出块为单位，不做逐字、逐行动画。普通展开状态和滚动像素不持久化；只记所在步骤。

### 4.2 小测

V1 只实现单选题。判断题是两选项单选；“预测输出”和“找错误原因”同样采用单选，避免 Kindle 输入长文本。

选项使用原生 radio，整行标签可点击；无预选项。选中不等于提交。点击“提交答案”后，显示“回答正确/回答错误”、正确选项以及每个选项的解释。错误不能只靠红色表示。

未选择就提交时显示行内提示，不计次数。一次提交只记一次，提交后锁定该次作答；刷新或返回该题恢复已提交结果，不重复计数。“重新作答”清空界面选择并隐藏解析，但在新答案提交前不删除旧记录。

答错进入待复习列表；此后显式重新作答答对，才移出待复习列表。查看解释、翻页、重新打开或标记课程已读都不能自动消除错题。再次答错仍留在列表。

复习页不复制一份题库，也不另做出题引擎。条目指向原课，例如 `lessons/dicts.html?review=1#s-quiz-1`；进入后先恢复该题已有记录，用户点击“重新作答”才开始新尝试，完成后提供返回复习页链接。不得仅因刷新该地址就重置作答或增加次数。复习模式不覆盖正常学习的全局继续位置，也不修改本课阅读步骤。

### 4.3 已读与实践

“已读”由用户在末步主动标记，不因访问页面、翻到末尾或答对而自动设置；支持取消已读。标记已读后不自动跳转，提供下一课链接。

已读不是掌握证明。课程已读、题目正确与实践完成是三种独立状态；未做小测或实践不阻止标记已读。

实践步骤包含任务、输入输出示例、验收清单、可展开提示和参考解法，以及“我已在 PyCharm 完成”复选框。状态只依据用户勾选；不上传代码、不自动判题、不检查电脑文件。

## 5. 课程规划与写作标准

### 5.1 V1 首发目录

下列 ID 是永久标识，显示编号由目录顺序生成，不将编号写进 ID。

| 模块 | 四节首发微课（ID：主题） | 模块实践 |
|---|---|---|
| M1 读懂基本代码 | `run-and-bindings`：运行脚本、变量绑定；`types-and-none`：基本类型、转换与 None；`strings`：字符串、切片与格式化；`conditions`：布尔表达式与条件分支 | 根据给定数据输出格式化费用说明，并处理简单条件 |
| M2 操作常用数据 | `loops`：for/while 与循环边界；`lists-tuples`：列表与元组；`dicts`：字典读取、更新与遍历；`sets`：集合、成员判断与去重 | 对给定记录做去重与频次统计 |
| M3 写出可解释的函数 | `functions`：参数与返回值；`scope-defaults`：作用域与默认参数；`alias-copy`：可变性、别名、浅拷贝及 is/==；`comprehensions-sort`：简单推导式与排序 key | 将记录清洗拆成函数并按指定字段排序 |
| M4 从片段走向脚本 | `exceptions`：读 traceback 与处理异常；`paths-text`：pathlib、UTF-8 文本及 with；`modules-venv`：模块、入口、虚拟环境与安装依赖；`tests-boundaries`：断言、边界用例与可测试函数 | 读取文本生成词频报告，并验证空文件等边界情况 |

每节只有一个主学习目标，关联语法服务于该目标。较长内容拆成更多短步骤，而非把整节塞进一个内容块。四个实践分别附在 `conditions`、`sets`、`comprehensions-sort`、`tests-boundaries`。

### 5.2 后续内容路线，不属于首发验收

按“工程基础补齐 → 数据处理 → API → 自动化 → 面试专项”扩展。工程基础补齐类与实例、迭代器/生成器、类型标注、测试和日志；数据处理先 CSV/JSON，再按任务引入 pandas，NumPy 只补必要部分；API 覆盖 HTTP、JSON、超时、状态码、分页和环境变量中的凭据；自动化覆盖目录批处理、命令行参数、dry-run 和重复执行安全；面试专项覆盖复杂度、哈希计数、双指针、二分及边界分析。

路线保存在仓库文档中。未完成课程不得作为可点击空页面上线，也不得计入进度分母。

### 5.3 教材质量约束

以 Python 3.13 为统一示例和测试基线；这是一项项目版本选择，不代表最新版本判断。基础语言语义以官方文档为参考。[R8]

讲解使用中文，保留必要英文术语。每步正文以 100—250 个汉字为目标，避免超过 350 字；代码一般 4—10 行，超过 12 行须拆分或说明必要性。代码尽量控制在 40 个等宽显示列内，中文按两列估算。

例子优先使用文件名、日志、记录、计数和小型数据清洗，不用数学推导掩盖编程基础。每课至少明确一个容易误解的机制、一项实际用途或面试关联；小测检查代码理解而非冷僻语法记忆。每道错选项必须解释具体错因。

遵循“先预测，再展开结果；先尝试，再看解法”。总结增加一句可口头回答的检查问题，便于拿到 ChatGPT 继续讨论，不增加 AI 接口或一键聊天功能。

所有声称可运行的示例必须验证实际输出；故意报错的例子必须标明异常类别与原因。不要使用未解释的随机性、集合显示顺序或依赖外部服务的响应作为固定答案。实际运行环境、必要前置知识和输入数据须明确。

## 6. 教材格式与内容协议

### 6.1 唯一内容来源

采用 **YAML 结构 + 多行 Markdown 正文**，一课一个 YAML 文件。这样既能编写解释和代码块，也能明确表达步骤、选项和答案。正文通过 YAML 的 `|` 多行字符串保存；UTF-8 编码；禁止原始 HTML、自定义脚本与嵌套组件。

课程只在内容文件维护。禁止将题干、答案、课程顺序复制进 JavaScript。新增课程只应需要添加内容文件、更新目录和相应内容测试，不改阅读器逻辑。

### 6.2 必须实现的字段

| 对象 | 字段及规则 |
|---|---|
| `course.yaml` | `schemaVersion: 1`、`courseId: python-relearn`、`title`、`pythonVersion: "3.13"`、`modules[]`；每个模块有 `id`、`title`、有序 `lessonIds[]` |
| 单课 | `schemaVersion: 1`、`id`、`title`、`goal`、`revision`、`status`、`prerequisites[]`、`steps[]`；`status` 只允许 `draft/published` |
| 通用步骤 | `id`、`type`、`title`；步骤 ID 在课内唯一；只支持下面四种类型 |
| `read` | `bodyMd`：说明、误区或总结，支持普通 Markdown 与代码块 |
| `reveal` | `bodyMd`：先显示的示例或问题；`revealLabel`：按钮文字；`revealMd`：展开后的结果或解释 |
| `quiz` | `questionId`、`revision`、`promptMd`、`options[]`、`correctOptionId`、`explanationMd`；每个选项有 `id`、`textMd`、`explanationMd`；2—4 个选项，且恰有一个正确项 |
| `practice` | `taskMd`、`hintMd`、`solutionMd`、`acceptance[]`；一课最多一个，只设一个整体完成状态，不逐条保存清单 |

ID 仅允许小写英文字母、数字与短横线；课程 ID 全站唯一，题目 ID 全站唯一，选项 ID 在题内唯一。题目建议使用 `<lessonId>-q1` 这样的标识，不能使用数组下标作为永久身份。目录中的模块归属和顺序是唯一依据。

构建器必须检查：字段类型和必填项、未知步骤类型、重复 ID、缺失引用、正确答案不存在、空解释、非法状态、目录中缺课、前置课不存在或不在前面，以及未完成内容误标为 published。报错必须包含文件、步骤或题目 ID，不允许静默跳过。首发数量要求另作为发布测试，内容协议本身不能硬编码为永远 16 课。

### 6.3 修改与版本

改标题、文字或课程排序不得改变 ID；目录调整不重置进度。普通内容修订递增课程 `revision`，已读记录仍保留。

题目含义、正确答案或选项内容发生影响作答的改变时，必须递增该题 `revision`。旧版本记录不得恢复成新题答案；在复习页标记“题目已更新，需重新作答”。首次提交新版本时替换该题的旧版本作答记录，次数从新版本重新统计。

删除的课程或题目不参与当前统计，但不用自动擦除其本地记录。已保存步骤被删除时回到该课第一步。新课程只扩大已发布课程总数，不改变已有已读记录。

## 7. 本地状态与容错

### 7.1 保存内容

使用一个命名空间键：`python-relearn:<siteId>:state`。`siteId` 是项目配置中的稳定唯一字符串，避免同一 GitHub Pages 域名下其他项目相互覆盖。不要调用 `localStorage.clear()`。

状态结构如下；示例仅定义协议，不是网页实现：

```json
{
  "schemaVersion": 1,
  "courseId": "python-relearn",
  "settings": {"fontSize": 26},
  "lastLocation": {"lessonId": "dicts", "stepId": "quiz-1"},
  "lessons": {
    "dicts": {
      "lastStepId": "quiz-1",
      "completed": false,
      "practiceDone": false
    }
  },
  "questions": {
    "dicts-q1": {
      "revision": 1,
      "attempts": 1,
      "wrongAttempts": 1,
      "lastOptionId": "b",
      "lastCorrect": false,
      "needsReview": true
    }
  }
}
```

没有课记录为“未开始”；有记录且 `completed=false` 为“学习中”；`completed=true` 为“已读”。未保存任何学习位置时 `lastLocation=null`，两个记录对象为空。实践状态只对含实践的课程有效。

已读进度＝当前已发布课中 completed 的数量/当前已发布课总数；错题记录不能修改这个比例。待复习集合为当前存在且已有作答记录，并满足“`needsReview=true` 或题目版本不匹配”的题，按题目去重。

只保存状态，不保存教材全文、DOM、完整作答历史或日志。切步骤、提交答案、标记已读、勾选实践及修改字号时立即保存；不依赖退出事件、定时保存或持续监听滚动。单次作答必须防止重复提交计数。

### 7.2 保存失败与备份

localStorage 按 origin 隔离，读取也可能因浏览器策略抛异常；因此“支持 JavaScript”不等于“必然能长期保存”。不同设备不会自动共享记录，清除站点数据或更换域名可能导致无法找回原记录。[R3]

初始化时在 try/catch 内检查读取和写入；所有后续读写同样防护。不可用或空间不足时，保留本页阅读、自测和临时状态，持续显示“本地保存不可用；刷新或跨页面后，临时记录可能丢失”。不得把多页面之间的内存状态当成持久保存，也不得显示保存成功。

已有数据损坏或 schemaVersion 不认识时，不自动覆盖；允许查看原始文本备份和显式重置。课页仍可在只读或本页临时模式使用。

设置页必须提供原生 textarea 文本备份：导出只显示 JSON，不修改状态；导入粘贴 JSON，检查课程 ID、版本、类型、字段范围和不超过 128 KiB 的 UTF-8 大小，再预览已读/复习数量，二次确认后整份替换，不做合并。无效输入不能破坏原记录；写入失败不能提示成功。未知但格式合法的历史课程/题目 ID 可保留，但不计入当前统计。

备份操作不依赖 Clipboard API、文件上传或 Kindle 文件下载能力。清除学习记录需要明确确认，只处理本项目键，并保留字号设置。此功能是人工备份恢复，不是云同步。

## 8. 技术架构与部署

### 8.1 构建与运行分开

构建端使用 Python 3.13，加 PyYAML、Jinja2、markdown-it-py 三项直接依赖；锁定经过测试的依赖版本。Python 只在开发电脑和 GitHub Actions 中执行，Kindle 不安装或执行这些依赖。

流程：内容校验 → Markdown 转 HTML → 模板生成页面和必要元数据 → 复制样式/脚本 → 输出 `dist/`。构建器只负责本项目四种步骤，不设计通用插件系统。

PyYAML 使用安全加载；模板明确启用 HTML 自动转义；Markdown 禁用原始 HTML。markdown-it-py 的默认 HTML 行为不能当作安全过滤器。备份输入只能用于解析状态，不能进入 `innerHTML`。[R5][R6][R7]

浏览器只使用静态 HTML、CSS、经典原生 JavaScript 和本地状态。每课所需内容已经在 HTML 中；首页、目录和复习页所需标题、ID、链接也在构建时生成，不运行时 fetch 全站教材或题库。

建议目录：

```text
content/course.yaml
content/lessons/*.yaml
content/fixtures/
templates/
assets/reader.js
assets/styles.css
tools/build.py
tools/validate.py
tests/
docs/content-authoring.md
docs/qa.md
requirements-build.txt
.github/workflows/pages.yml
dist/
```

README 必须提供安装、校验、构建、运行测试、本地 HTTP 预览及发布步骤。不得用直接双击 `file://` 页面代替正式运行测试。[R3]

### 8.2 渐进增强

原始 HTML 即完整可读。只有事件绑定、步骤解析等初始化成功后，才启用“只显示当前步”的 CSS 类和交互按钮。初始化失败必须恢复静态显示，不能留下所有内容都隐藏的白页。

关闭或阻断 JavaScript 时：正文、例子、题目和解析仍能阅读；当前课以完整短文章显示并可用锚点跳转；解析放在清晰标注的答案区域，不默认挤在题干旁。隐藏不可工作的提交和保存按钮，显示不保存状态的说明。无脚本模式只承诺阅读，不承诺分步或记分。

保存模块失败不能阻断分步和答题模块。已完整载入的当前课，断网后仍可在该页面内切步和自测；跨课、刷新及重新打开不承诺离线可用。

### 8.3 GitHub Pages

使用 GitHub Actions 构建并发布 `dist/`，使用 GitHub 官方 Pages 配置、上传产物与部署 actions；最小化权限，按官方工作流要求配置部署环境与 Pages/OIDC 权限。[R9]

设统一 `basePath` 配置，支持 `/` 和 `/仓库名/` 两种部署路径；所有内部链接、资源和 404 返回入口均通过同一 URL 生成逻辑处理，不能手写固定根路径。必须有真实的每课 HTML 文件，不使用 SPA 的 404 重定向技巧。

CSS/JS 文件名加构建内容 hash，避免新 HTML 与旧脚本混用。部署不改动 localStorage 的键或 ID。站点自身不增加统计、广告或追踪脚本；任何外部 API 密钥均不得进入公开产物。后续 API 教材中的请求在 PyCharm 执行，网页只展示代码和预先核验的示例响应。

## 9. Kindle 显示与兼容底线

### 9.1 视觉与操作

单栏，正文最大宽度约 760 CSS px；窄屏自动占满可用宽度，左右留 16—24 px。默认白底黑字，无背景图、灰色小字、阴影、渐变、图标字体和远程字体。中文使用系统字体，代码使用系统等宽字体。

正文默认 26 CSS px，提供 22/26/30 三档，行高约 1.7；代码不低于正文字号的 85%，其他辅助说明不低于 20 px。正文和重要提示目标对比度至少 7:1。按钮和选项点击区至少 48×48 CSS px，间隔至少 8 px；此目标比 W3C 的 44×44 增强触控目标更保守。[R4]

代码保留缩进，优先从教材源头缩短行。允许代码块单独横向滚动；不允许整页横向滚动，不为了塞下代码把字体缩小，也不强行折行造成缩进歧义。

不依赖 hover、拖拽、滑动手势、双击、长按或精细点击。保留原生按钮、链接、radio、checkbox、textarea 与清晰焦点样式。步骤切换、答案展开都必须是用户主动触发；不轮播、不倒计时、不自动跳页。

### 9.2 实现限制

使用 UTF-8、`lang=zh-CN` 和正确 viewport；允许用户缩放。不使用固定高度正文容器、固定/吸顶工具栏或全屏布局锁定。

基础布局采用普通文档流，不依赖 CSS Grid、复杂 Flex 布局、CSS 变量或新选择器才能成立。JavaScript 交付产物遵守 ES5 语法，使用经典 script；不把 ES modules、fetch、Promise、async/await、现代 Clipboard API、IndexedDB 或 Service Worker 作为核心前提。API 不可用时做功能降级，而不是下载大型补丁库。

不用用户代理字符串判断功能，采用实际能力检查。正常浏览模式是验收目标；Kindle 的文章模式仅尽力保持可读，不承诺交互或进度功能。

### 9.3 体积预算

每页必要资源的未压缩总体积不超过 100 KiB：HTML 不超过 60 KiB，共用 JavaScript 不超过 30 KiB，CSS 不超过 10 KiB。初次必要资源原则上只有 HTML/CSS/JS，不请求第三方资源；这些预算不包含用户主动打开的练习文件。

同课切步骤与答题不发网络请求，不预加载全课程，不持续运行计时器。这些是开发预算，不是对尚未测试的实机速度作保证。

## 10. 验收、执行顺序与交付物

### 10.1 可重复验收

| 编号 | 必须通过的场景 |
|---|---|
| A1 | 新浏览器显示开始学习；已有记录显示正确的继续位置和已读数量 |
| A2 | 学到中间步骤后刷新、退出浏览器再打开，正常存储环境下仍可继续；浏览器前进/后退与直接锚点一致 |
| A3 | 不选答案不记次数；一次提交只记一次；刷新不重复计数；错误进入复习，查看解析不移除，显式重做答对才移除 |
| A4 | 已读、答题和实践状态互不冒充；已读可撤销，实践可取消勾选 |
| A5 | 导出后清除再导入可恢复；错误课程、畸形数据、未知版本或超限文本不覆盖现有记录；原型污染等异常键被拒绝 |
| A6 | 禁用脚本、阻断脚本下载、存储读取抛错、写入抛错、损坏 JSON 均不会让教材白屏，也不会虚报保存成功 |
| A7 | 课程改名/重排后进度保留；题目升版不沿用旧答案；删除步骤、课程和题目不会导致页面崩溃或错误统计 |
| A8 | 根路径及项目子路径均可直接访问所有课程与锚点、刷新并加载资源；不存在 SPA 兜底依赖和失效站内链接 |
| A9 | 320/600/800/1024 CSS px 宽度及最大字号下无正文裁切、遮挡或整页横滚；键盘可操作；代码缩进正确 |
| A10 | 完成 16 课、32 道有完整解析的小测和 4 项实践；所有示例输出及参考解法经过测试；未发布内容不进入进度 |
| A11 | 构建体积预算通过；同课切步与答题无网络请求；当前已载入课在断网后仍能操作 |

构建与内容校验使用自动测试；浏览器回归可用 Playwright，检查功能、截图、链接、资源请求和存储故障。对浏览器交付脚本进行 ES5 语法检查。教材可运行片段建立测试映射，记录其所在课、输入、实际输出或预期异常；文件题使用本地固定样本和临时目录，测试不得修改个人文件。纯说明片段明确标注不能独立运行。

### 10.2 实机验收不能省略

在用户实际 Kindle Scribe 上，记录型号/代次、固件、网络和日期，至少验证：打开实际 GitHub Pages 地址、三档字号、连续切步、展开答案、选项提交、代码块滚动、错题重做、睡眠唤醒、关闭后重新打开以及保存恢复。确认运行环境能真正访问该部署地址。

桌面缩小窗口、模拟慢速网络和设备模拟只能作为预检，不等于 Kindle 实测。无实机条件时可交付“桌面已验收、Kindle 待验收”版本，不得写“Kindle 完全兼容”。实机发现阻断阅读、无法准确点击或正常环境下无法保存，应列为阻断项，不靠增加更多功能回避。

### 10.3 Coding Agent 的实施顺序

先完成 **1 节代表性样课 + 静态构建 + GitHub Pages 预览**，样课要覆盖代码、展开、两道题和本地保存，用它尽早验证 Kindle；没有实机时明确保留待验收标记，不阻断其他可验证工作。

接着完成全部页面、状态协议、导入校验和故障降级；再按统一格式填充 16 节教材与测试；最后完成路径、内容、存储、体积及实机验收。不要先写大批课程，再发现阅读器在设备上无法操作。

最终交付：可构建的仓库、完整首发教材、构建和测试脚本、Pages 工作流、README、内容编写指南、测试报告与 Kindle 实测/待测记录。没有发布授权或权限时，交付工作流和配置说明，不虚构线上地址。

**完成标准：学习者能够在 Kindle 上稳定地读完一个短知识点、完成一次自测、下次继续，并能在 PyCharm 中验证自己是否真正会用。不是功能数量最多，也不是把看过的内容全部标成“已掌握”。**

## 参考资料

以下资料用于事实核验；字号、课程规模、技术选择和验收阈值是本项目的设计决策。核验日期：2026-09-23。

- [R1] Amazon Kindle Scribe User Guide，Web Browser 与显示技术章节。`https://d1ergij2b6wmg5.cloudfront.net/kug/kindle_scribe/v1/en-US/html/kug.html`
- [R2] GitHub Docs，What is GitHub Pages? `https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages`
- [R3] MDN，Window: localStorage property。`https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage`
- [R4] W3C，Target Size (Enhanced) 与 Contrast (Enhanced)。`https://www.w3.org/WAI/WCAG22/Understanding/target-size-enhanced.html`；`https://www.w3.org/WAI/WCAG22/Understanding/contrast-enhanced.html`
- [R5] markdown-it-py，Security。`https://markdown-it-py.readthedocs.io/en/latest/security.html`
- [R6] Jinja，API / Autoescaping。`https://jinja.palletsprojects.com/en/stable/api/`
- [R7] PyYAML Documentation。`https://pyyaml.org/wiki/PyYAMLDocumentation`
- [R8] Python 3.13 Tutorial。`https://docs.python.org/3.13/tutorial/`
- [R9] GitHub Docs，Using custom workflows with GitHub Pages。`https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages`