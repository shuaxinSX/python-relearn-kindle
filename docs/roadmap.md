# 后续内容路线（不属于 V1 验收）

沿用开发规格 §5.2，尚未开发的课程不生成空页面、不加入进度分母：

1. 工程基础：类与实例、迭代器/生成器、类型标注、测试、日志。
2. 数据处理：先 CSV/JSON，再按任务引入 pandas；NumPy 只补必要部分。
3. API：HTTP、JSON、超时、状态码、分页、环境变量中的凭据。
4. 自动化：目录批处理、命令行参数、dry-run、重复执行安全。
5. 面试专项：复杂度、哈希计数、双指针、二分、边界分析。

这是保留路线，不承诺交付时间，也不扩大当前 16 课/32 题/4 实践的验收范围。
